# Kick: applies immediately (no confirmation gate).
#
# INPUT : $(player) -> exact player name, $(reason) -> kick reason
$function macroengine:core/internal/cmd/kick_apply {player:"$(player)",reason:"$(reason)"}

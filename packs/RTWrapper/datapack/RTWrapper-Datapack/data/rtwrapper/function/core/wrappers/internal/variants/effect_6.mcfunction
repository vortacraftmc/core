# RTWrapper generated 6-parameter macro variant for /effect.
# Parameters: action, targets, effect, duration, amplifier, hide_particles
$effect $(action) $(targets) $(effect) $(duration) $(amplifier) $(hide_particles)
